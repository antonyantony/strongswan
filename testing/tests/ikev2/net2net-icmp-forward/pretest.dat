alic:e::iptables-restore < /etc/iptables.rules
# moon::iptables-restore < /etc/iptables.rules # fix the rules
moon::ip route add 10.2.0.8/30 via PH_IP_SUN
sun::ip route add PH_IP_ALICE/32 via PH_IP_MOON
winnetou::ip route add PH_IP_ALICE/32 via PH_IP_MOON
alice::systemctl daemon-reload
moon::systemctl daemon-reload
alice::systemctl start strongswan
moon::systemctl start strongswan
alice::expect-connection rw
moon::expect-connection rw
alice::ping -c 3 -W 1 -i 0.2 PH_IP_BOB
